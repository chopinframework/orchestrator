/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oraclePostApiContextMultipart } from "../../funcs/oraclePostApiContextMultipart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApiContextMultipartRequestBody$inboundSchema,
};

export const tool$oraclePostApiContextMultipart: ToolDefinition<typeof args> = {
  name: "oracle_post-api-context-multipart",
  description: `Create a new context entry by request nonce

Creates a new context entry for a request identified by its nonce`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oraclePostApiContextMultipart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
